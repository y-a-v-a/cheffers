## Build artifacts
/target/

## Rust tooling
**/*.rs.bk

## Dependency lockfile (keep if this is a binary crate)
Cargo.lock

## Web playground JS dependencies (the bundle is committed; deps are not)
docs/editor/node_modules/

.claude

.vscode