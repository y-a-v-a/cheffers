{
  "name": "cheffers-playground",
  "version": "0.2.0",
  "private": true,
  "description": "Browser playground for the Cheffers Chef interpreter",
  "type": "module",
  "scripts": {
    "build": "esbuild editor.js --bundle --format=esm --minify --external:./pkg/* --outfile=editor.bundle.js"
  },
  "devDependencies": {
    "codemirror": "^6.0.1",
    "esbuild": "^0.25.0"
  }
}
